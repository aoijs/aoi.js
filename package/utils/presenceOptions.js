module.exports = {
    id: "The ID of the user that updated the presence;.id", 
    status: "The status of the user;.status",
    activities: "The activities for this user;.activities.map(c => c.name).join(', ')", 
    guildID: "The ID of the guild the user updated the presence on;guild.id",
}